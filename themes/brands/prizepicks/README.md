# PrizePicks theme

Brand colors from the Visual Identity guide (Stankonia, ATLien, So Fresh, So Clean, etc.). **Dark is the default** for both Marp decks (`theme: prizepicks` → `themes/prizepicks.css`) and matplotlib charts when using `load_theme("prizepicks")`.

Typography: Aktiv Grotesk (falls back to system sans if not installed).
